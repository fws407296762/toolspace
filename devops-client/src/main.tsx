import * as React from 'react';
import * as ReactDOM from "react-dom";

const bpp = (abc:String)=>{
  return (
    <h1>最牛逼的第一个项目</h1>
  )
};


ReactDOM.render(bpp("1212"),document.getElementById("app"))